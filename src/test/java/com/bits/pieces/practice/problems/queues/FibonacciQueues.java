package com.bits.pieces.practice.problems.queues;

/**
 * TODO - Add Class Definition
 *
 * @author Nate Vardell
 * @since 8/17/2019
 */
public class FibonacciQueues {
    /**
     * IntQueue q = new IntQueue();
     * q.enqueue(0);
     * q.enqueue(1);
     * for (int i = 0; i < 10; i++) {
     *     int a = q.dequeue();
     *     int b = q.dequeue();
     *     q.enqueue(b);
     *     q.enqueue(a + b);
     *     System.out.println(a);
     * }
     */
}
